
#=====================================
# chargemet des packages ----
#=====================================


# 1. exploration des données

library(DataExplorer)     # Automated data exploration process for analytic tasks and predictive modeling,
library(rmarkdown)        # Convert R Markdown documents into a variety of formats.
library(naniar)           # provides data structures and functions that facilitate the plotting of missing values and examination of imputations.
library(tidyverse)        # This package is designed to make it easy to install and load multiple 'tidyverse' packages in a single step.
library(gt)               # Easily Create Presentation-Ready display tables from tabular data with an easy-to-use set of functions
library(labelled)         # Manipulating Labelled Data
library(forcats)          #	Helpers for reordering factor levels( manualy collapsing levels )
library(moments)          # skewness and kurtosis fonction
library(lubridate)        # Functions to work with date-times and time-spans
library(rlang)            # A toolbox for working with base types, core R features and 'Tidyverse' features.
library(broom)            # Summarizes key information about statistical objects in tidy tibbles

library(ggplot2)          # Create Elegant Data Visualisations Using the Grammar of Graphics
library(ggridges)         #  provide a convenient way of visualizing changes in distributions over time or space.
library(ggtext)           # for adding text on graphique
library(ggstats)          # for proportion in bar plot
library(ggpubr)           # gghistogram() / provides some easy-to-use functions for creating and customizing 'ggplot2'- based publication ready plots.
library(patchwork)        # providing mathematical operators for combining multiple plots.
library(highcharter)      # charting library offering numerous chart types with a simple configuration syntax.
library(plotly)           # Create interactive web graphics from 'ggplot2' graphs and/or a custom interface
library(ComplexHeatmap)   # Complex heatmaps are efficient to visualize associations between different sources of data sets and reveal potential patterns
library(visNetwork)       # allows an interactive visualization of networks.
library(circlize)

# 2. tools 

library(styler)           # Pretty-prints R code without changing the user's formatting intent.
library(RColorBrewer)     # forbeautiful color palette
library(rlang)            # as_label()
library(glue)             # for adding r variable in (html or string) element on graphics with ggtext


# 3. machine learning

library(xgboost)          # Extreme Gradient Boosting
library(rpart)            # Recursive Partitioning and Regression Trees
library(Metrics)          # Evaluation Metrics for Machine Learning
library(MASS)             # stepAIC()
library(glmnet)           # efficient procedures for fitting the entire lasso or elastic-net regularization path for  GLM

# 4. shiny verse

library(shiny)            # build interactive web applications with R
library(bs4Dash)          # Use of  dashboard template 'AdminLTE3' built on top of 'Bootstrap 4'

library(shinyjs)          # common useful JavaScript operations in Shiny apps that will greatly improve your apps without having to know any JavaScript.
library(esquisse)         # use esquise to quickly import data and view it in Panel

# library(semantic.dashboard)
# library(shiny.semantic)


# 5. UI component

library(shiny.fluent)      # set of UI components for building Shiny applications
library(shinyWidgets)      # winbox(), inputSweetAlert() /	Collection of custom input controls and user interface components for 'Shiny' applications.
library(htmltools)         # Tools for HTML generation and output.
library(htmlwidgets)       # framework for creating HTML widgets

library(bslib)             # Simplifies custom 'CSS' styling of both 'shiny
library(fresh)             # use_theme() , create_theme() # Customize 'Bootstrap' and 'Bootswatch' themes,

library(waiter)            # Full screen and partial loading screens for 'Shiny' with spinners,
library(shinycssloaders)   # withspinner()
library(spsComps)          # animation to a Shiny element
library(shinyFeedback)

library(DT)                # JavaScript Library 'DataTables'












